# chezmoi:template:left-delimiter="{{", right-delimiter="}}"
$ErrorActionPreference = "Stop"

{{ if eq .chezmoi.os "windows" -}}

$sw = [System.Diagnostics.Stopwatch]::StartNew()
Write-Host "Fast deploying configurations to Scoop persist directories..." -ForegroundColor Cyan

# ----------------------------
# 0) Force 开关
# ----------------------------
$forceRaw = [string]$env:CHEZMOI_SCOOP_FORCE
$forceRefresh = @("1", "true", "yes", "on") -contains $forceRaw.Trim().ToLowerInvariant()
if ($forceRefresh) {
    Write-Host "  Force refresh enabled (CHEZMOI_SCOOP_FORCE=$forceRaw)" -ForegroundColor Yellow
}

# ----------------------------
# 1) Scoop 根目录
# ----------------------------
$scoopRoot = $env:SCOOP
if ([string]::IsNullOrWhiteSpace($scoopRoot)) {
    $scoopRoot = Join-Path $env:USERPROFILE "scoop"
}
$scoopGlobalRoot = $env:SCOOP_GLOBAL
if ([string]::IsNullOrWhiteSpace($scoopGlobalRoot)) {
    $scoopGlobalRoot = "C:\ProgramData\scoop"
}

# ----------------------------
# 2) 读取 mapping
# ----------------------------
$appsJson = @'
{{ .scoop.apps | toJson }}
'@
$apps = $appsJson | ConvertFrom-Json
$sourceDir = "{{ .chezmoi.sourceDir }}"

# ----------------------------
# 3) 读取缓存 state
# ----------------------------
$stateDir = Join-Path $env:LOCALAPPDATA "chezmoi"
$statePath = Join-Path $stateDir "scoop-deploy-state.json"
if (-not ([System.IO.Directory]::Exists($stateDir))) {
    New-Item -ItemType Directory -Path $stateDir -Force | Out-Null
}

$state = @{
    version = 1
    entries = @{}
}

if (([System.IO.File]::Exists($statePath)) -and (-not $forceRefresh)) {
    try {
        $loaded = Get-Content $statePath -Raw | ConvertFrom-Json -AsHashtable
        if ($loaded -and $loaded.version -eq 1 -and $loaded.entries) {
            $state = $loaded
        }
    } catch {
        Write-Host "  State file is invalid, rebuilding cache..." -ForegroundColor Yellow
    }
}

# 全局统计
$totalScanned = 0
$totalUpdated = 0
$totalSkipped = 0

# ----------------------------
# 4) 主循环
# ----------------------------
foreach ($app in $apps) {
    # 先确定该应用是否安装在 Global 目录 (优化：使用 .NET 方法加速验证)
    $userAppPath = Join-Path $scoopRoot "apps\$($app.scoop_app)"
    $globalAppPath = Join-Path $scoopGlobalRoot "apps\$($app.scoop_app)"
    $isGlobal = (-not ([System.IO.Directory]::Exists($userAppPath)) -and ([System.IO.Directory]::Exists($globalAppPath)))

    # 判断是否配置了 app_path (如果未配置，PowerShell 中该属性为 $null)
    if ($null -ne $app.app_path) {
        # --- 模式 A: 部署到 app 目录 ---
        $appBase = if ($isGlobal) {
            Join-Path $scoopGlobalRoot "apps\$($app.scoop_app)\current\app"
        } else {
            Join-Path $scoopRoot "apps\$($app.scoop_app)\current\app"
        }

        # 将 app_path 作为相对路径拼接到 app 目录下 (处理空字符串的情况)
        if ([string]::IsNullOrWhiteSpace($app.app_path)) {
            $targetRoot = $appBase
        } else {
            $targetRoot = Join-Path $appBase $app.app_path
        }
        Write-Host "[$($app.name)] Target mode: App Directory ($targetRoot)" -ForegroundColor DarkGray
    } else {
        # --- 模式 B: 传统的 persist 目录 (后备默认) ---
        $persistBase = if ($isGlobal) {
            Join-Path $scoopGlobalRoot "persist\$($app.scoop_app)"
        } else {
            Join-Path $scoopRoot "persist\$($app.scoop_app)"
        }
        $targetRoot = Join-Path $persistBase $app.target_base
    }

    $appSourceRoot = Join-Path $sourceDir $app.source_base

    if (-not ([System.IO.Directory]::Exists($appSourceRoot) -or [System.IO.File]::Exists($appSourceRoot))) {
        Write-Host "  Skipping $($app.name): source '$appSourceRoot' not found." -ForegroundColor DarkGray
        continue
    }

    # 判断是目录还是单文件
    $isDir = [System.IO.Directory]::Exists($appSourceRoot)
    $fileList = @()

    if (-not $isDir) {
        # 单文件模式：直接将该文件加入映射
        $fileList = @(Get-Item $appSourceRoot)
    } else {
        # 目录模式：读取 includes 规则
        $includes = $app.includes
        # 如果没有定义 includes 或数组为空，默认扫描该目录下所有文件
        if ($null -eq $includes -or $includes.Count -eq 0) {
            $includes = @('**')
        }

        foreach ($pattern in $includes) {
            if ($pattern -eq '**') {
                $fileList += Get-ChildItem -Path $appSourceRoot -File -Recurse
            } else {
                $searchPath = Join-Path $appSourceRoot $pattern
                $fileList += Get-ChildItem -Path $searchPath -File -ErrorAction SilentlyContinue
            }
        }

        # 处理 excludes 排除逻辑
        $excludes = $app.excludes
        if ($null -ne $excludes -and $excludes.Count -gt 0) {
            $fileList = $fileList | Where-Object {
                $relPath = $_.FullName.Substring($appSourceRoot.Length).TrimStart('\', '/').Replace('\', '/')
                $matched = $false
                foreach ($exPattern in $excludes) {
                    $normPattern = $exPattern.Replace('\', '/')
                    # 匹配规则：完全匹配、作为目录前缀、作为后缀、或者包含在路径中间
                    if ($relPath -like $normPattern -or
                        $relPath -like "$normPattern/*" -or
                        $relPath -like "*/$normPattern" -or
                        $relPath -like "*/$normPattern/*") {
                        $matched = $true
                        break
                    }
                }
                return (-not $matched) # 如果匹配到了排除规则，则返回 false 过滤掉
            }
        }
    }

    $appScanned = 0
    $appUpdated = 0
    $appSkipped = 0

    # 使用 Sort-Object 保证唯一，且不破坏 FileInfo 对象结构
    if ($fileList.Count -gt 1) {
        $fileList = $fileList | Sort-Object -Property FullName -Unique
    }

    foreach ($file in $fileList) {
        $appScanned++
        $totalScanned++

        $isTmpl = $file.Extension -eq '.tmpl'

        # 根据单文件/目录决定如何计算目标路径
        if (-not $isDir) {
            $targetFile = $targetRoot
            $targetRelPath = Split-Path $targetRoot -Leaf
        } else {
            $relPath = $file.FullName.Substring($appSourceRoot.Length).TrimStart('\', '/').Replace('\', '/')
            $targetRelPath = if ($isTmpl) { $relPath -replace '\.tmpl$', '' } else { $relPath }

            if ([string]::IsNullOrEmpty($targetRelPath)) {
                $targetFile = $targetRoot
            } else {
                $targetFile = Join-Path $targetRoot $targetRelPath.Replace('/', [System.IO.Path]::DirectorySeparatorChar)
            }
        }

        $targetParent = [System.IO.Path]::GetDirectoryName($targetFile)
        if (-not ([System.IO.Directory]::Exists($targetParent))) {
            New-Item -ItemType Directory -Path $targetParent -Force | Out-Null
        }

        # 获取文件元数据
        $srcLen = [int64]$file.Length
        $srcTicks = [int64]$file.LastWriteTimeUtc.Ticks

        $tgtFileInfo = $null
        $tgtLen = 0
        $tgtTicks = 0
        if ([System.IO.File]::Exists($targetFile)) {
            $tgtFileInfo = [System.IO.FileInfo]::new($targetFile)
            $tgtLen = [int64]$tgtFileInfo.Length
            $tgtTicks = [int64]$tgtFileInfo.LastWriteTimeUtc.Ticks
        }

        # 缓存键名增加 app_path，解决多路径映射下的缓存冲突
        $entryKey = "$($app.name)|$($app.app_path)|$targetRelPath"
        $cached = $state.entries[$entryKey]

        # ----------------------------------------------------
        # 签名快筛 (Fast Skip)
        # ----------------------------------------------------
        $fastSkip = $false
        if (-not $forceRefresh -and $cached -and $tgtFileInfo) {
            $expectedMode = if ($isTmpl) { "tmpl" } else { "copy" }
            if (
                $cached.mode -eq $expectedMode -and
                $cached.srcLen -eq $srcLen -and
                $cached.srcTicks -eq $srcTicks -and
                $cached.tgtLen -eq $tgtLen -and
                $cached.tgtTicks -eq $tgtTicks
            ) {
                $fastSkip = $true
            }
        }

        if ($fastSkip) {
            $appSkipped++
            $totalSkipped++
            continue
        }

        # ----------------------------------------------------
        # 降级验证与写入 (Slow Path / Fallback)
        # ----------------------------------------------------
        $didWrite = $false

        if ($isTmpl) {
            # 模板文件：渲染并对比内容
            $renderedContent = (Get-Content $file.FullName -Raw | chezmoi execute-template) -join "`r`n"
            $shouldWrite = $true

            if ($tgtFileInfo -and -not $forceRefresh) {
                $existingContent = [System.IO.File]::ReadAllText($targetFile, [System.Text.Encoding]::UTF8)
                if ($existingContent -eq $renderedContent) {
                    $shouldWrite = $false
                }
            }

            if ($forceRefresh -or $shouldWrite) {
                [System.IO.File]::WriteAllText($targetFile, $renderedContent, [System.Text.Encoding]::UTF8)
                Write-Host "  -> Rendered: $($app.name)/$targetRelPath" -ForegroundColor Green
                $didWrite = $true
            }
        } else {
            # 普通文件：算 Hash 兜底 (MD5 算法比 SHA256 更快)
            $shouldWrite = $true
            if ($tgtFileInfo -and -not $forceRefresh) {
                $srcHash = (Get-FileHash $file.FullName -Algorithm MD5).Hash
                $tgtHash = (Get-FileHash $targetFile -Algorithm MD5).Hash
                if ($srcHash -eq $tgtHash) {
                    $shouldWrite = $false
                }
            }

            if ($forceRefresh -or $shouldWrite) {
                Copy-Item -Path $file.FullName -Destination $targetFile -Force
                Write-Host "  -> Copied: $($app.name)/$targetRelPath" -ForegroundColor Green
                $didWrite = $true
            }
        }

        if ($didWrite) {
            $appUpdated++
            $totalUpdated++
        } else {
            $appSkipped++
            $totalSkipped++
        }

        # ----------------------------------------------------
        # 更新状态缓存
        # ----------------------------------------------------
        $finalTgtItem = [System.IO.FileInfo]::new($targetFile)
        $state.entries[$entryKey] = @{
            mode     = if ($isTmpl) { "tmpl" } else { "copy" }
            srcLen   = $srcLen
            srcTicks = $srcTicks
            tgtLen   = [int64]$finalTgtItem.Length
            tgtTicks = [int64]$finalTgtItem.LastWriteTimeUtc.Ticks
        }
    }

    if ($appScanned -eq 0) {
        Write-Host "  [$($app.name)] no files matched." -ForegroundColor DarkGray
    } elseif ($appUpdated -gt 0) {
        Write-Host "  [$($app.name)] updated $appUpdated files, skipped $appSkipped." -ForegroundColor Cyan
    } else {
        Write-Host "[$($app.name)] all $appSkipped files are up to date." -ForegroundColor DarkGray
    }
}

# ----------------------------
# 5) 写回缓存 state
# ----------------------------
$stateJson = $state | ConvertTo-Json -Depth 8
[System.IO.File]::WriteAllText($statePath, $stateJson, [System.Text.Encoding]::UTF8)

$sw.Stop()
Write-Host ("Done. scanned={0}, updated={1}, skipped={2}, elapsed={3:n2}s" -f $totalScanned, $totalUpdated, $totalSkipped, $sw.Elapsed.TotalSeconds) -ForegroundColor Magenta

{{- end }}
